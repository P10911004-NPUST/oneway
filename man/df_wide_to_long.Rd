% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_data.R
\name{df_wide_to_long}
\alias{df_wide_to_long}
\title{Convert a data frame from wide to long format}
\usage{
df_wide_to_long(df, columns, names_to = "grp", values_to = "val", keep = FALSE)
}
\arguments{
\item{df}{A data frame.}

\item{columns}{A numeric or character vector specifying the columns to
pivot into long format.}

\item{names_to}{A character string specifying the name of the new column
containing the original column names. Default is \code{"grp"}.}

\item{values_to}{A character string specifying the name of the new column
containing the values from the pivoted columns. Default is \code{"val"}.}

\item{keep}{Logical. If \code{TRUE}, columns not specified in \code{columns} are
retained in the output. If \code{FALSE} (default), only the pivoted
columns are returned.}
}
\value{
A data frame in long format. The output contains one column storing the
original column names (\code{names_to}) and another storing the corresponding
values (\code{values_to}). If \code{keep = TRUE}, non-pivoted columns are retained.
}
\description{
Reshapes a data frame from wide format to long format by stacking one or more columns into
a pair of key-value columns. This increases the number of rows while reducing the number of
columns. For more advanced reshaping operations, consider using \code{tidyr::pivot_longer()}.
}
\examples{
n <- 10
df0 <- data.frame(
  G1 = stats::rnorm(n, 6, 1),
  G2 = stats::rnorm(n, 6, 1),
  G3 = stats::rnorm(n, 3, 1)
)

df_wide_to_long(df0, c("G1", "G2"))

}

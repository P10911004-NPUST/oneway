% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_balance}
\alias{is_balance}
\title{Check Balance of Sample Sizes Among Groups}
\usage{
is_balance(data, formula, buffer_ratio = 0.2)
}
\arguments{
\item{data}{A data frame or a list containing observations from each group.}

\item{formula}{A formula specifying the dependent variable (DV) and the
independent variable (IV) in the form \code{DV ~ IV}. This argument is required
when \code{data} is a data frame and is ignored when \code{data} is already a list.}

\item{buffer_ratio}{Numeric value between 0 and 1 (default: 0.2). The allowable
proportional deviation from the mean sample size.
For example, \code{buffer_ratio = 0.2} allows group sizes to differ from the
mean by up to 20\%. When \code{buffer_ratio = 0}, the function requires exact
equality of sample sizes among groups.}
}
\value{
A logical value:
\describe{
\item{TRUE}{Sample sizes among groups are considered balanced.}
\item{FALSE}{At least one group has a sample size outside the specified
tolerance range.}
}
}
\description{
Determines whether sample sizes are approximately balanced across groups.
Sample sizes are considered balanced when the smallest and largest group
sizes are within the specified tolerance range relative to the average
sample size.
}
\details{
The function compares each group's sample size with the mean sample size
across groups. The sample sizes are considered balanced if:

\deqn{
(1-r) \leq \frac{\min(n)}{\bar{n}} \leq
\frac{\max(n)}{\bar{n}} \leq (1+r)
}

where \eqn{\bar{n}} is the mean sample size across groups and \code{r} is \code{buffer_ratio}.
}
\examples{
is_balance(list(rnorm(10), rnorm(13)), buffer_ratio = 0.2)
is_balance(list(rnorm(10), rnorm(13)), buffer_ratio = 0.1)
}
